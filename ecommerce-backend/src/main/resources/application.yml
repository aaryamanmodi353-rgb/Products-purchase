spring:
  application:
    name: ecommerce-backend

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

---
# Development Profile
spring:
  config:
    activate:
      on-profile: dev
  data:
    mongodb:
      uri: mongodb+srv://deepmodi670_db_user:rentmojo12345@cluster0.1smantl.mongodb.net/technova?appName=Cluster0

---
# Production Profile
spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      uri: ${DATABASE_URL}

server:
  port: ${PORT:8080}
